# 用户手册：快速编辑站点配置

本博客的所有主要设置都集中在同一个文件里：**`src/config.ts`**。
日常改站名、换头像、换壁纸、改番剧数据，都不需要碰其他代码，只需编辑这一个文件。
下面按配置分组说明"在哪里改什么"。

> 提示：修改后保存即可；若修改的是数据文件路径/番剧数据，可能需要重新运行 `pnpm build` 才会生效。

---

## 1. 站点基本信息

在 `src/config.ts` 顶部的 `SITE_LANG` / `SITE_TIMEZONE`，以及 `siteConfig` 开头部分：

| 配置项 | 作用 | 示例 |
| --- | --- | --- |
| `SITE_LANG` | 站点语言 | `"zh_CN"`、`"en"`、`"ja"` |
| `SITE_TIMEZONE` | 时区（UTC 偏移小时，-12 ~ 12） | `8` 表示 UTC+8 |
| `title` | 站点标题（浏览器标签、页脚） | `"FGmagi Blog"` |
| `subtitle` | 站点副标题 | `"少女祈祷中"` |
| `siteURL` | 站点网址 | `"https://fgmagi.pages.dev"` |
| `siteStartDate` | 站点开始运行日期（统计运行天数） | `"2026-2-18"` |

**导航栏**：`siteConfig.navbarTitle` 控制顶栏左侧的标题/Logo。
`mode: "logo"` 只显示 Logo，`mode: "text-icon"` 显示图标+文字；
`icon` / `logo` 填图片路径（相对 `public/`），留空时会使用 `navbarLogoFallback` / `navbarIconFallback` 兜底。
导航菜单结构在 `navBarConfig.links` 中编辑（含"目录"下拉里的追番/友链/图片/我的/关于）。

**主题色**：`siteConfig.themeColor.hue` 是主题色色相（0-360），例如红 0、青 200、粉 345；
`fixed: false` 表示允许访问者在页面上自行切换主题色（`true` 则隐藏选择器）。

---

## 2. 壁纸与横幅

所有横幅/壁纸图片路径都写在 `src/config.ts` 的 `fullBanner` 和 `wideBanner` 两个对象里：

- `fullBanner`：**全屏壁纸**使用的图片（默认生效）。
- `wideBanner`：**顶部横幅**使用的图片（默认不生效，仅当壁纸模式为 `banner` 时）。

每个对象都有 `desktop`（桌面）和 `mobile`（移动）两组，填以 `/` 开头的网页路径即可，
例如 `"/assets/images/A-FS.webp"`，也可填多个实现轮换。

**壁纸模式**：`siteConfig.wallpaperMode.defaultMode` 三选一：

- `"fullscreen"`：全屏壁纸（默认）
- `"banner"`：顶部横幅
- `"none"`：无壁纸

**全屏壁纸细节**：`fullscreenWallpaperConfig` 中：

| 配置项 | 作用 | 示例 |
| --- | --- | --- |
| `src.desktop` / `src.mobile` | 桌面/移动壁纸图片 | `fullBanner.desktop` |
| `position` | 图片位置（`top`/`center`/`bottom`） | `"center"` |
| `carousel.enable` | 是否轮播 | `true` |
| `carousel.interval` | 最小轮换间隔（秒），切换间距不足则跳过 | `300` |
| `opacity` | 壁纸透明度（0-1） | `0.28` |
| `blur` | 背景模糊程度 | `1` |

顶部横幅的轮播、水波纹、首页文字、图片来源等细节在 `siteConfig.banner` 内调整。

---

## 3. 字体（font 组）

`siteConfig.font` 统一管理字体：

| 配置项 | 作用 | 示例 |
| --- | --- | --- |
| `webDir` | 字体文件所在目录（相对 `public/` 的网页路径） | `"/assets/font"` |
| `faces` | 页面要注册的 @font-face 列表（`family` 字体族名、`files` 文件列表、`weight` 字重） | 见下方说明 |
| `asciiFont` | 英文字体：`fontFamily` 字体族、`localFonts` 本地字体文件、`enableCompress` 是否压缩 | `fontFamily: "system-ui"` |
| `cjkFont` | 中日韩字体（回退），配置同上；`enableCompress: true` 时构建会生成 woff2 子集 | `localFonts: ["思源黑体.ttf"]` |
| `useSegoeUI` | 全局字体栈是否包含 "Segoe UI" | `true` / `false` |

**给页面增加一个自定义字体**：把字体文件放进 `public/assets/font/`，然后在 `faces` 里加一项，例如：

```ts
{ family: "我的字体", files: ["我的字体.woff2", "我的字体.ttf"], weight: "400", style: "normal" }
```

**压缩注意事项（与 `scripts/compress-fonts.js` 兼容）**：
构建时只有 `cjkFont.enableCompress = true` 且 `localFonts` 非空才会生成 woff2 子集；
字体文件名必须与 `public/assets/font/` 下的文件完全一致（区分大小写），否则构建会报错提示。
建议生产用 woff2 优先、dev 用 ttf 兜底（即 `files` 里 woff2 写在 ttf 前面）。

---

## 4. 番剧页面

**数据模式**：`siteConfig.anime.mode` 三选一：

- `"bilibili"`：B站数据（默认）
- `"bangumi"`：Bangumi 数据
- `"local"`：本地 JSON（`dataFiles.animeJson`）

**追番页布局**：`siteConfig.anime.defaultLayout` 默认 `"regular"`（常规列表板式）；
改为 `"compact"` 后，追番页默认以紧凑封面网格展示（封面大小不变，每行列数按页面宽度自动为 2~6 个）。
访问者仍可在追番页右上角切换"常规/紧凑"，浏览器会记住其选择（localStorage 键 `animeLayoutMode`）。

**账号配置**：

| 配置项 | 作用 |
| --- | --- |
| `bilibili.vmid` | 你的 Bilibili 用户 ID（vmid） |
| `bilibili.SESSDATA` | 浏览器 Cookie 里的 SESSDATA（可选，用于获取观看进度） |
| `bilibili.coverMirror` | 封面图镜像源（可选，如 `"https://images.weserv.nl/?url="`） |
| `bilibili.useWebp` | 是否使用 WebP 封面 |
| `bilibili.fetchOnDev` | 是否在开发模式下抓取 B 站数据 |
| `bangumi.userId` | 你的 Bangumi 用户 ID |
| `bangumi.fetchOnDev` | 是否在开发模式下抓取 Bangumi 数据 |

**更新数据命令**（在项目根目录执行）：

```bash
pnpm run update-bilibili   # 刷新 B 站番剧数据到 public/data/bilibili-data.json
pnpm run update-bangumi    # 刷新 Bangumi 数据到 src/data/bangumi-data.json
```

**空状态文案**：`siteConfig.anime.emptyMessages` 里可以修改"未配置账号/数据为空"时页面显示的文字。

---

## 5. 友链页面布局（friendCardLayout）

友链页（`/friends/`）同一行显示的卡片数量随浏览器窗口宽度变化，
档位映射配置在 `siteConfig.friendCardLayout.breakpoints`（数组需按 `minWidth` 升序排列）。

| 配置项 | 作用 |
| --- | --- |
| `minWidth` | 浏览器宽度临界值（单位 px，需递增）；达到该宽度后切换到此档位 |
| `columns` | 该档位同一行显示的卡片数（1-4，页面会自动夹紧到该范围） |

默认档位：

```ts
breakpoints: [
  { minWidth: 0, columns: 1 },    // 基础档：<640px 每行 1 张
  { minWidth: 640, columns: 2 },  // ≥640px 每行 2 张
  { minWidth: 1280, columns: 3 }, // ≥1280px 每行 3 张
  { minWidth: 1600, columns: 4 }, // ≥1600px 每行 4 张
]
```

修改示例（900px 起变 2 张、1440px 起变 4 张）：

```ts
breakpoints: [
  { minWidth: 0, columns: 1 },
  { minWidth: 900, columns: 2 },
  { minWidth: 1280, columns: 3 },
  { minWidth: 1440, columns: 4 },
]
```

保存刷新后即按新档位显示，无需改动其它代码。

---

## 6. 文章列表与标签

`siteConfig.postListLayout`：

- `defaultMode`：`"list"` 单列列表 / `"grid"` 双列网格
- `pageSize`：每页文章数量（5-10）

`siteConfig.tagStyle.useNewStyle`：`true` 用悬停高亮新样式，`false` 用外框常亮旧样式。
分页相关在文章列表页自动按 `pageSize` 生效，无需单独设置。

**中间卡片悬停变暗**（`postCardHoverConfig`，在 `src/config.ts` 中编辑）：

- `hoverDarkness`：悬停时先变暗到的暗度（%，0=不变暗，100=全黑；默认 10）
- `holdDuration`：到达悬停暗度后的停顿时间（毫秒，默认 600）
- `restoredDarkness`：停顿后回落并保持的暗度（%，默认 5，即悬停暗度的一半）
- `restoreDuration`：恢复速度——从悬停暗度过渡到恢复暗度用时（毫秒，默认 400；鼠标移出恢复为不暗亦用此时长）

首页中间卡片标签为纯展示：不可点击、不可选中复制、悬停不变色（无需配置）。

---

## 7. 侧边栏组件（sidebarLayoutConfig）

`sidebarLayoutConfig` 控制左右侧栏显示哪些组件、位置与动画：

- `properties`：每个组件的详细配置（`type` 组件类型、`position` 顶部/粘性、`animationDelay` 动画延迟毫秒数）
- `components.left` / `components.right` / `components.drawer`：左侧栏/右侧栏/抽屉栏各显示哪些组件
- `defaultAnimation`：组件入场动画开关与延迟
- `responsive.breakpoints`：移动端 768px / 平板 1280px / 桌面 1280px 三个断点

常用组件类型：`profile`（个人资料）、`announcement`（公告）、`categories`（分类）、
`tag-filter`（标签筛选，26.09.02 [13] 新增）、`tags`（标签）、`site-stats`（站点统计）、`calendar`（日历）。

**标签筛选卡片（`tag-filter`）**：显示在首页左下角（原分类卡片空位）。标签按字母序以 chip
排列（Flex 行内拉伸、最后一行靠左），点击标签即激活并**就地过滤**中间文章卡片（命中任一激活标签的文章显示，
OR 语义，仅过滤当前页已渲染卡片）；再次点击已激活标签取消；切换页面 / 原地跳回主页 / 翻页会自动清空激活态。
标签数据不含 `active: false` 的文章。

`tagFilterConfig`（`src/config.ts`）控制卡片细节：

- `tagFilterConfig.showCount`：`false`（默认）仅显示标签名；`true` 时每个标签 chip 内同时显示文章数，
  形式为"标签名：N"。

卡片是否出现在侧栏、位置与折叠阈值由 `sidebarLayoutConfig` 中 `type: "tag-filter"` 的属性控制
（`components.left` 中移除该类型即可关闭标签筛选卡）。

---

## 8. 数据文件位置一览表

这些路径统一配置在 `src/config.ts` 的 **`dataFiles`** 中，修改后全站读取位置同步生效。
"文件系统路径"相对项目根目录；"网页路径"以 `/` 开头、相对 `public/`。

| 配置项 | 内容 | 文件系统路径（默认） | 网页路径 |
| --- | --- | --- | --- |
| `animeJson` | 番剧-本地模式数据 | `public/data/anime.json` | `/data/anime.json` |
| `bilibiliDataJson` | B 站番剧数据（更新脚本输出） | `public/data/bilibili-data.json` | `/data/bilibili-data.json` |
| `bilibiliDataJsonLegacy` | B 站旧数据（仅读取兜底） | `src/data/bilibili-data.json` | — |
| `bangumiDataJson` | Bangumi 番剧数据（更新脚本输出） | `src/data/bangumi-data.json` | — |
| `friendsJson` | 友链数据 | `public/data/friends.json` | `/data/friends.json` |
| `postsDir` | 文章内容目录 | `./public/data/posts` | `/data/posts/...` |
| `specDir` | 独立页面内容（关于/我的等） | `./public/data/spec` | `/data/spec/...` |
| `albumsDir` | 相册图片目录 | `public/images/albums` | — |
| `albumsWebDir` | 相册网页路径前缀 | — | `/images/albums` |
| `faviconIco` | 默认 favicon | `public/favicon/favicon.ico` | `/favicon/favicon.ico` |
| `fontDir` | 字体文件目录 | `public/assets/font` | `/assets/font` |

---

## 9. 杂项开关（评论/分享/统计/公告/音乐/樱花/看板娘）

| 配置 | 作用 | 默认 |
| --- | --- | --- |
| `commentConfig.enable` | 文章评论功能（Twikoo） | 关 |
| `shareConfig.enable` | 文章分享功能 | 关 |
| `umamiConfig.enabled` | Umami 站点统计（脚本在 `umamiConfig.scripts`） | 关 |
| `announcementConfig` | 侧边栏公告：标题/内容/是否可关闭/链接 | 内容"施工中，暂无告示" |
| `musicPlayerConfig` | 音乐播放器（`mode`：`meting`/`local`，填歌单 ID 与音乐源） | 关 |
| `sakuraConfig` | 樱花特效（数量、大小、透明度、速度） | 关 |
| `pioConfig` | 看板娘（模型路径、位置、对话文本） | 关 |
| `tagFilterConfig.showCount` | 标签筛选卡：标签 chip 内显示文章数（"标签名：N"） | 关 |
| `footerConfig` | 页脚自定义 HTML（如备案号） | 关 |
| `licenseConfig` | 文章许可信息 | CC BY-NC-SA 4.0 |
| `permalinkConfig` | 固定链接格式（`format` 支持 `%year%`/`%postname%` 等） | 关 |
| `toc` | 文章目录（开关/位置/深度/假名序号） | 开，侧边栏，深度 2 |
| `generateOgImages` | 生成 OpenGraph 分享图（构建很慢） | 关 |
| `showCoverInContent` / `showLastModified` | 文章内显示封面 / 显示"上次编辑" | 开 |

---

## 10. 死代码说明（[死代码]）

`src/config.ts` 中标了 **`[死代码]`** 的配置项可以改，但**当前不会生效**，避免误以为改动无效：

- `wideBanner.desktop`：桌面横幅图源已清空（修复首帧闪现），保留结构供后续启用。
- `siteConfig.banner.carousel`：轮播未启用（`enable: false` 时横幅从数组中随机选一张）。
- `siteConfig.postListLayout.allowSwitch`：布局切换按钮已隐藏，保留字段不影响现有行为。

---

## 11. 常见修改清单

- 改站名 → `siteConfig.title` / `siteConfig.navbarTitle.text`
- 换头像 → `profileConfig.avatar`
- 换壁纸 → `fullBanner`（全屏）或 `wideBanner`（横幅）+ `wallpaperMode.defaultMode`
- 改主题色 → `siteConfig.themeColor.hue`
- 换字体 → `siteConfig.font.faces` + 把字体文件放入 `public/assets/font/`
- 改番剧账号 → `siteConfig.bilibili` / `siteConfig.bangumi`，然后运行对应 `pnpm run update-*`
- 改友链每行卡片数 → `siteConfig.friendCardLayout.breakpoints`（见第 5 节）
- 增删侧边栏组件 → `sidebarLayoutConfig.properties` 与 `components`
- 改数据文件位置 → `dataFiles` 对应项
